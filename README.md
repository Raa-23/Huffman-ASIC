# Huffman Encoding — Hardware Implementation in Verilog

A hardware-based lossless text compression and decompression system implemented in Verilog, targeting the SkyWater 130nm (sky130) PDK. The design encodes and decodes English text using a predefined Huffman tree optimized for English character frequency distribution.

---

## Overview

Huffman Encoding assigns shorter binary codewords to more frequent characters and longer codewords to rarer ones, achieving efficient lossless compression. This project implements the full encode-decode pipeline as synthesizable RTL, verified through a file-driven Verilog testbench, and targeted for ASIC implementation using an open-source EDA flow.

---
<!--
## Repository Structure

```
├── decoder.v          # Dual-mode RTL module (encoder + decoder, SEL-controlled)
├── decoder_TB.v       # File-driven Verilog testbench
├── encoding           # Huffman lookup table (28-bit binary entries)
├── textfile           # Input ASCII text for encoding
├── outputfile.txt     # Compressed binary output (encoder) / input (decoder)
├── outputfile2.txt    # Recovered ASCII output (decoder)
└── README.md
```
-->


## Architecture

The system is built around a single dual-mode RTL module controlled by the `SEL` signal.

### Dual-Mode Module (`decoder.v`)

| SEL | Mode    | Input              | Output                        |
|-----|---------|--------------------|-------------------------------|
| 0   | Encoder | 8-bit ASCII char   | 16-bit Huffman code + length  |
| 1   | Decoder | 16-bit encoded bits| 8-bit recovered ASCII char    |

### Encoding Table Format

Each entry in the `encoding` file is a 28-bit word:

```
[27:24] — Code length (4 bits)
[23:8]  — Huffman code, left-justified (16 bits)
[7:0]   — ASCII character value (8 bits)
```
<!--
### End-of-Transmission

The system uses ASCII `ETX` (`0x03`) as the end-of-transmission sentinel. The encoder sets `stop=1` upon encountering this character; the decoder halts bit consumption accordingly.

---

## Signal Description

| Signal          | Direction | Width  | Description                                  |
|-----------------|-----------|--------|----------------------------------------------|
| `SEL`           | Input     | 1      | Mode select: 0=Encode, 1=Decode              |
| `CLK2`          | Input     | 1      | Clock signal (manually toggled by testbench) |
| `reset`         | Input     | 1      | Synchronous reset                            |
| `input_ascii`   | Input     | 8      | ASCII character input (encoder mode)         |
| `input_enc`     | Input     | 16     | Packed Huffman bits input (decoder mode)     |
| `encoding`      | Input     | 28     | Current encoding table entry                 |
| `output_ascii`  | Output    | 8      | Recovered ASCII character (decoder mode)     |
| `output_encoding`| Output   | 16     | Huffman code output (encoder mode)           |
| `output_length` | Output    | 4      | Length of Huffman code (encoder mode)        |
| `match_found`   | Output    | 1      | High when a table match is found             |
| `stop`          | Output    | 1      | High when ETX sentinel is encountered        |
| `give_input`    | Output    | 1      | Requests next 16-bit input chunk (decoder)   |

---
-->

## How It Works

### Encoding (SEL=0)

1. Testbench reads one ASCII character at a time from `textfile`
2. Testbench scans the `encoding` file entry by entry, presenting each 28-bit entry to the DUT
3. DUT compares `input_ascii` against `encoding[7:0]`; on match, outputs the Huffman code and length
4. Testbench writes the Huffman bits to `outputfile.txt`
5. Process repeats until ETX (`0x03`) is encountered

### Decoding (SEL=1)

1. Testbench reads the compressed bitstream from `outputfile.txt`, assembling 16-bit chunks
2. DUT accumulates bits in a 32-bit internal buffer
3. Testbench scans the `encoding` file; DUT compares the top bits of its buffer against each entry
4. On match, DUT outputs the recovered ASCII character and removes the consumed bits from the buffer
5. Testbench writes the recovered character to `outputfile2.txt`

<!--
---

## Testbench (`decoder_TB.v`)

The testbench is fully file-driven and event-controlled:

- **Three clocks:** `CLK3` (file reading), `CLK1` (input assembly), `CLK2` (DUT clock)
- **`CLK2` is manually toggled** — making simulation event-driven rather than free-running
- Reads input from files, drives the DUT, and writes output back to files
- Validates correct operation by checking `match_found` and `stop` flags

### Files Required to Run

| File           | Description                              |
|----------------|------------------------------------------|
| `textfile`     | Plain ASCII input text to be encoded     |
| `encoding`     | Huffman lookup table in 28-bit binary format |
| `outputfile.txt` | Auto-generated by encoder; read by decoder |

---
-->

## EDA Tool Flow

| Stage                  | Tool                        |
|------------------------|-----------------------------|
| Simulation             | Icarus Verilog (iverilog)   |
| Waveform Viewing       | GTKWave                     |
| Synthesis              | Yosys + sky130 PDK          |
| Place and Route        | OpenROAD                    |
| Physical Verification  | Magic                       |
| PDK                    | SkyWater 130nm (sky130)     |

---
<!--
## Running Simulation

```bash
# Compile
iverilog -o sim.out decoder.v decoder_TB.v

# Run
vvp sim.out

# View waveforms
gtkwave waveforms.vcd
```

---

## Target Device

- **PDK:** SkyWater 130nm (sky130)
- **Standard Cell Library:** sky130_fd_sc_hd (high-density)
- **Target Frequency:** 50 MHz
- **EDA Flow:** OpenROAD open-source RTL-to-GDS

---

## Status

| Milestone                        | Status      |
|----------------------------------|-------------|
| Huffman table generation         | Complete    |
| Dual-mode RTL (encoder/decoder)  | Complete    |
| File-driven testbench            | Complete    |
| Counter module                   | In Progress |
| Top-level integration            | In Progress |
| Synthesis (Yosys)                | Pending     |
| Place and Route (OpenROAD)       | Pending     |
| Physical verification (Magic)    | Pending     |

---

## Author

This project is part of an ASIC design course implementation targeting the open-source sky130 PDK using the OpenROAD RTL-to-GDS flow.
